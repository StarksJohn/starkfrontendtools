/**
 * 获取vue的路由页面参数
 * Get route page parameters of Vue
 * @param $route
 */
const queryPageParam= ($route)=> {
    console.log('vueTools.ts queryPageParam $route=,', $route)
}

export default {
    queryPageParam
}
